-- ============================================================
-- V3: P1 检索能力
-- 为分类分面 / 分类过滤添加索引（LIKE '%q%' 无法走 B-tree，全文检索后续用 FULLTEXT）
-- ============================================================

CREATE INDEX idx_books_category ON books (category);
